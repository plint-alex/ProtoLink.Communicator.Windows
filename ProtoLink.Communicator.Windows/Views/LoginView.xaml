<UserControl x:Class="ProtoLink.Communicator.Windows.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400"
             Foreground="{DynamicResource ForegroundBrush}">
    <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch">
        <StackPanel HorizontalAlignment="Stretch">
            <TextBlock Text="Login" Margin="0,0,0,6" Foreground="{DynamicResource ForegroundMutedBrush}"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource IgTextBox}" Margin="0,0,0,12"/>
            <TextBlock Text="Password" Margin="0,0,0,6" Foreground="{DynamicResource ForegroundMutedBrush}"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,12" Padding="12,10" Height="40"
                         PasswordChanged="PasswordBox_PasswordChanged"/>
            <Button Content="Log in" Command="{Binding LoginCommand}" IsDefault="True"
                    Style="{DynamicResource IgPrimaryButton}" HorizontalAlignment="Stretch" Margin="0,4,0,10"/>
            <ProgressBar IsIndeterminate="True" Height="4" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFED4956" TextWrapping="Wrap" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
