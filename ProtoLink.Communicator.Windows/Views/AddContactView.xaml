<UserControl x:Class="ProtoLink.Communicator.Windows.Views.AddContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Foreground="{DynamicResource ForegroundBrush}">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Command="{Binding CloseCommand}" HorizontalAlignment="Right" VerticalAlignment="Top"
                Width="30" Height="30" Content="✕" Background="Transparent" BorderThickness="0" FontSize="18" ToolTip="Close"
                Foreground="{DynamicResource ForegroundBrush}"
                Visibility="{Binding IsEmbedded, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0">
                <TextBlock Text="Add Contact" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"
                           Visibility="{Binding IsEmbedded, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                <TextBlock Text="User ID" Margin="0,0,0,5"/>
                <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="30" VerticalContentAlignment="Center"/>
                <Button Content="Add Contact" Command="{Binding AddContactCommand}" Height="30" Margin="0,0,0,20"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusMessage, Converter={StaticResource StatusColorConverter}}"
                           TextWrapping="Wrap" Margin="0,0,0,10"
                           Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
