<UserControl x:Class="ProtoLink.Communicator.Windows.Views.MessengerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
             Foreground="{DynamicResource ForegroundBrush}">
    <UserControl.Resources>
        <Style x:Key="ChatListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <ContentPresenter HorizontalAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="{DynamicResource ContentBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox ItemsSource="{Binding Contacts}" SelectedItem="{Binding SelectedContact}" Grid.Column="0" Margin="5"
                 Background="{DynamicResource ListBackground}" BorderBrush="{DynamicResource BorderBrushStandard}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Margin="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <ListBox x:Name="MessagesList"
                         ItemsSource="{Binding Messages}"
                         Background="{DynamicResource ChatBackgroundBrush}"
                         BorderBrush="{DynamicResource BorderBrushStandard}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.ScrollChanged="OnMessagesScrollChanged"
                         ItemContainerStyle="{StaticResource ChatListBoxItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Telegram-style: message bubble only (no inline date/time) -->
                            <Grid Margin="4,2" MaxWidth="520">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="HorizontalAlignment" Value="Left"/>
                                        <Setter Property="Margin" Value="4,2,56,2"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFromMe}" Value="True">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Margin" Value="56,2,4,2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Border CornerRadius="12" Padding="10,8">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource ChatBubbleIncomingBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFromMe}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource ChatBubbleOutgoingBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Text}"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource ChatBubbleTextBrush}"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Date chip — visible only while scrolling (Telegram) -->
                <Border x:Name="StickyDateChip"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0,8,0,0"
                        Padding="12,4"
                        CornerRadius="12"
                        Background="{DynamicResource ChatDateChipBackgroundBrush}"
                        Visibility="Collapsed"
                        IsHitTestVisible="False">
                    <TextBlock x:Name="StickyDateText"
                               FontSize="12"
                               Foreground="{DynamicResource ChatDateChipForegroundBrush}"/>
                </Border>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                         Grid.Column="0" Margin="0,0,5,0" AcceptsReturn="True" MinHeight="36"
                         VerticalContentAlignment="Center"/>
                <Button Content="Send" Command="{Binding SendCommand}" Grid.Column="1" Width="60" Margin="0,0,5,0"/>
                <Button Content="Receive" Command="{Binding ReceiveCommand}" Grid.Column="2" Width="60"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
